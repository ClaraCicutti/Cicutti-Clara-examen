server:
  port: ${PORT:8082}
  
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/examenFinalBack
      
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry:true
    register-with-eureka:true
